{
  "$id": "https://revit-suite/schemas/level_report.schema.json",
  "title": "LevelReportInput",
  "x-version": "1.0.0",
  "type": "object",
  "properties": {
    "schemaVersion": {
      "type": "string",
      "default": "1.0.0"
    },
    "includeLinkedModels": {
      "type": "boolean",
      "default": true,
      "description": "Whether to include level information from linked Revit models."
    },
    "precision": {
      "type": "integer",
      "minimum": 0,
      "maximum": 6,
      "default": 2,
      "description": "Decimal precision for the elevation in feet."
    },
    "maxPreviewRows": {
      "type": "integer",
      "minimum": 0,
      "maximum": 20,
      "default": 5,
      "description": "Number of rows to include in the response preview payload."
    }
  },
  "additionalProperties": false
}
